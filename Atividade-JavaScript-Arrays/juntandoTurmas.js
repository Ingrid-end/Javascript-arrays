const salaDePython = ['Carolina', 'Ana', 'Rodrigo'];
const salaJavascript = ['Isabely', 'Lucas', 'Edson'];

// Metodo faz a concatenação entre o elemento e o parametro passado pelo metedo
const salasUnificadas = salaDePython.concat(salaJavascript);
console.log(salasUnificadas);