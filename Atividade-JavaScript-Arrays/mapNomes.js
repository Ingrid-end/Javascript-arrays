let nomes = ["ana Julia", "Caio vicinios", "BIA silva"];
                                                // Metodo q deixa tudo(string) com letra maiúsculas
const nomesAtualizado = nomes.map(nome => nome.toUpperCase());

console.log(nomesAtualizado);